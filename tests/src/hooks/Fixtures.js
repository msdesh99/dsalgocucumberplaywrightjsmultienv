exports.fixtures = fixtures = {
    
    page : undefined,
    loginPage : undefined,
    linkedListPage : undefined,
    logger: undefined,
    recordcount :0,
    
    topic:undefined

    /* About topic value: 
      topic == module type for endtoend automation
      topic = excel for ddloginexcel
      topic = json for ddloginjson
    */  

}